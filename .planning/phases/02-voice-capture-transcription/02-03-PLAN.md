---
phase: 02-voice-capture-transcription
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - components/voice-note-capture.tsx
  - app/(protected)/dashboard/page.tsx
autonomous: false
user_setup:
  - service: qa-devices
    why: "Manual QA must include at least one iOS Safari device and one Chromium browser"

must_haves:
  truths:
    - "User can start/stop recording directly from dashboard"
    - "Duration is visible while recording"
    - "Upload/transcription progress and error states are visible"
    - "Transcript text is displayed after successful transcription"
  artifacts:
    - path: "components/voice-note-capture.tsx"
      provides: "Dashboard recording and transcription card"
      min_lines: 120
    - path: "app/(protected)/dashboard/page.tsx"
      provides: "Integration of voice capture card into authenticated dashboard"
      contains: "VoiceNoteCapture"
  key_links:
    - from: "components/voice-note-capture.tsx"
      to: "hooks/use-voice-recorder.ts"
      via: "record start/stop/reset and duration state"
      pattern: "useVoiceRecorder"
    - from: "components/voice-note-capture.tsx"
      to: "app/api/transcribe/route.ts"
      via: "multipart upload fetch call"
      pattern: "/api/transcribe"
---

<objective>
Ship the Phase 2 user experience by wiring recorder + transcription into the protected dashboard and exposing clear mobile-first states for start, stop, upload, success, and error.

Purpose: make the voice-to-transcript loop usable end-to-end for authenticated users.
Output: dashboard-level voice recording flow with transcript result.
</objective>

<execution_context>
@/Users/guapo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guapo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-voice-capture-transcription/02-01-PLAN.md
@.planning/phases/02-voice-capture-transcription/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build voice capture/transcription UI component</name>
  <files>
    components/voice-note-capture.tsx
  </files>
  <action>
    Create `components/voice-note-capture.tsx` (`'use client'`) with:
    - Primary actions:
      - Start recording
      - Stop recording
      - Reset
      - Transcribe
    - State display:
      - recording status badge
      - live duration while recording
      - file size after stop
      - uploading/transcribing indicator
      - error banner with retry guidance
      - transcript output area (readonly textarea or paragraph block)
    - Accessibility/mobile:
      - all buttons min-h-[44px]
      - clear labels and `aria-live` for dynamic status/error text

    Upload flow:
    - Build `FormData` with `audio` file name from MIME extension mapping
    - `fetch('/api/transcribe', { method: 'POST', body: formData })`
    - Parse JSON and display transcript
    - Handle non-2xx status responses with user-friendly messages
  </action>
  <verify>
    - `npm run lint`
    - `npm run build`
    - Manual browser test confirms start/stop/transcribe loop works
  </verify>
  <done>
    A reusable UI component performs end-to-end recording and transcription.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate recording component into dashboard</name>
  <files>
    app/(protected)/dashboard/page.tsx
  </files>
  <action>
    Update dashboard page to include `VoiceNoteCapture` near the top of the page:
    - Keep current welcome/sign-out structure intact
    - Add heading text for the recording workflow (e.g., "Capture meeting note")
    - Preserve mobile-first spacing and readability
  </action>
  <verify>
    Confirm dashboard renders:
    - Existing auth-dependent content (welcome email, sign out)
    - New recording/transcription module
  </verify>
  <done>
    Dashboard becomes a functional launch pad for Phase 2 workflow.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual cross-device QA for Phase 2 acceptance</name>
  <what-built>Dashboard recording flow with upload to Whisper and transcript display.</what-built>
  <how-to-verify>
    Prerequisites:
    - `.env.local` includes Supabase vars and `OPENAI_API_KEY`
    - User is authenticated

    Verification steps:
    1. Open `/dashboard` on desktop Chrome.
    2. Start recording; duration should increment.
    3. Stop recording; file metadata should appear.
    4. Transcribe; see loading indicator; transcript appears.
    5. Record an intentionally long clip or mocked large blob; oversized file error appears before upload.
    6. Repeat start/stop/transcribe on iOS Safari.
    7. Confirm no format errors and transcript completes.
  </how-to-verify>
  <resume-signal>Type "approved" or list observed issues by step number.</resume-signal>
</task>

</tasks>

<verification>
1. Dashboard has working record/transcribe UX
2. UX states are explicit for loading and failures
3. iOS Safari and desktop Chromium both complete the flow
</verification>

<success_criteria>
- VOICE-01 through VOICE-05 are satisfied end-to-end for MVP scope
- Phase 2 can be marked complete after human QA approval
</success_criteria>

<output>
After completion, create `.planning/phases/02-voice-capture-transcription/02-03-SUMMARY.md`
</output>
